name: Support Request
description: Get help or ask a question
labels: ["Type: Support", "-Needs: Triage"]
body:
  - type: dropdown
    id: category
    attributes:
      label: Category
      description: What type of support do you need?
      options:
        - How-to question
        - Account/access issue
        - Configuration help
        - Training request
        - General inquiry
        - Other
    validations:
      required: true

  - type: input
    id: application
    attributes:
      label: System/Application
      description: What system or application is this about? (pre-filled with this repo, edit if different)
      placeholder: "e.g., Web portal, Email, Network, QuickBooks"
      value: "enhanced-people"
    validations:
      required: false

  - type: textarea
    id: description
    attributes:
      label: How can we help?
      description: Describe what you need help with
      placeholder: "Please provide as much detail as possible"
    validations:
      required: true

  - type: textarea
    id: context
    attributes:
      label: Additional Context
      description: Any other information that might be helpful
      placeholder: "Screenshots, error messages, relevant details..."
    validations:
      required: false

  - type: dropdown
    id: urgent
    attributes:
      label: Urgent?
      description: Does this need immediate attention?
      options:
        - "No"
        - "Yes"
    validations:
      required: true
